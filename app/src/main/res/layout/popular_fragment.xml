<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:id="@+id/popularFilmLayout">

    <TextView
        android:layout_marginTop="150sp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="20sp"
        android:gravity="center"
        android:text="Movie Browser est une application Web pour en savoir plus sur les films, vous pouvez trouver la plupart des films sortis."
        android:textColor="@color/white"/>

    <TextView
        android:layout_marginTop="50sp"
        android:layout_width="match_parent"
        android:layout_height="50sp"
        android:text="Films Populaires"
        android:textColor="@color/white"
        android:gravity="center"
        android:textSize="30sp"
        />


        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/filmCardListView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="10sp"
            android:background="@drawable/round_image"
            tools:listitem="@layout/item_film_card"
            android:scrollbars="horizontal"
            android:scrollbarThumbHorizontal="@color/white"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
            android:orientation="horizontal"
            />


</LinearLayout>